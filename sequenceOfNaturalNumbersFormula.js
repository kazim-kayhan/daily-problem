const sequenceOfNaturalNumbers = (n) => {
  Math.abs(n);
  console.log(
    `The sequence of natural numbers up to ${n} is ${(1 / 2) * n * (n + 1)}`
  );
};
sequenceOfNaturalNumbers(2);
sequenceOfNaturalNumbers(3);
sequenceOfNaturalNumbers(4);
sequenceOfNaturalNumbers(5);
